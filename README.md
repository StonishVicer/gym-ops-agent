# gym-ops-agent

Claude API + MCP portfolio project over 100% synthetic gym data.

```bash
cp .env.example .env   # add your OpenRouter key
make setup
make test
```

See `CLAUDE.md` for architecture, commands, and conventions.
